###### 1.全居中的方法

弹性布局

宫格布局

CSS3偏移

###### 2。js的数据类型

基本数据类型：

数字，字符串，unll，undefined，boolean

引用数据类型：

数组，对象，Symbool

###### 3.js的选择器

getElenmentByID、getElenmentByName、getElenmentByClassName、getElenmentByTagName

###### 4.事件权柄

单击事件（onclick）

双击事件（ondbclick）

鼠标悬浮事件

鼠标进入事件

鼠标移出事件

##### 5.eval（）函数的作用

将字符串转化为js语句，还可以转化为JSON格式。